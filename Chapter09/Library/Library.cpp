#include "Library.h"
Library::Library()
{
    cout << "lib\n";
    checkOutBooks();
}
//when checkouting make sure there is a user and  books in class else: throw
//then be sure there is no fees else: throw
// if not create transaction and push to vector

void Library::checkOutBooks()
{
cout << "check\n";
}
